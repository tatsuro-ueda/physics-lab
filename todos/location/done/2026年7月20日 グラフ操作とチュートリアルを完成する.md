---
status: done
format: rough-dev
devStage: closing
priority: normal
scheduled: 2026-07-20T10:30:43+00:00
dateCreated: 2026-07-20T10:30:43+00:00
dateModified: 2026-07-20T10:30:43+00:00
tags:
  - task
  - archived
---

# 2026年7月20日 グラフ操作とチュートリアルを完成する

> 状態：devStage closing
> 次のゲート：なし

## 概要

- When: 2026年7月20日
- Where: `/home/exedev/physics-lab`
- Who: AIエージェント
- What: 加速度と位置のグラフ操作を共通化し、操作を必須チュートリアルへ接続する
- Why: 生徒が一般的なタッチ操作でグラフを調べ、教員がページごとの操作差を説明せずに済むようにするため

## Spec

### 決定事項

- `src/lab-common.js` をジェスチャー処理の正本とし、`build.py` で各配布HTMLへ埋め込む
- 拡大表示中は2本指で横軸と縦軸を拡大縮小し、1本指で両軸を移動する
- 加速度と位置の必須チュートリアルを、拡大、ピンチ、移動、値読み、復帰の順にそろえる
- 画面上で成立した操作とチュートリアル合格を、同じ `onPinch` / `onPan` イベントへ接続する
- 加速度(3)の正解文にはSVG文字列を連結せず、Safariでも文章だけを表示する

### 却下案

- ページごとに独自のポインター処理を追加する案
- 横軸だけをピンチ対象にする案
- 状態遷移図にないダブルタップリセットを残す案
- 正解文のSVGを二重解釈してSafariだけ回避する案

### 未決事項

- なし

### 完了条件

- [x] 拡大中のピンチとドラッグが横軸・縦軸の両方へ反映される
- [x] 加速度と位置で操作方法と案内がそろっている
- [x] 操作成功とチュートリアル進行が同じイベントを使う
- [x] 加速度(3)の正解文にSVGタグが残らない
- [x] 生成、テスト、ブラウザ確認、Caddy反映が完了している

### 設計レビュー

- 原本 checklist: `docs/development-checklist.md`
- /tmp コピー: `/tmp/development-design-checklist-20260720-1030-graph-gestures.md`
- レビュー結果: 共通部品を正本にすること、ページ間の操作を統一すること、表示上の成功と合格判定を一致させることを確認した
- 未通過時の扱い: 実装へ進まず Spec または Discussion へ戻す

## 作業ログ

- `src/lab-common.js` のピンチ距離を2次元化し、単指ドラッグで時間軸と値軸を移動するようにした
- `onPinch` / `onPan` を共通APIに追加し、加速度と位置のチュートリアルへ接続した
- 仕様にないダブルタップリセットを削除し、タップを拡大、値読み、復帰へ限定した
- 加速度と位置の拡大時ヒントを同じ4操作へそろえた
- 加速度チュートリアルへ一時停止後のピンチ、移動、値読み、復帰を追加した
- 位置チュートリアルへ拡大、ピンチ、移動、値読み、復帰を追加した
- ブラウザ確認中に未定義だった `refreshVisible` と `clearMarkers` の内部関数化を行った
- 加速度(3)の正解文末に連結されていた `TUT_ICONS.check` を削除した
- `python3 build.py` で `src/lab-common.js` を加速度、ジャイロ、磁気、位置の生成HTMLへ埋め込んだ
- 回帰テストは修正前の失敗と修正後の成功を確認し、全体で `33 tests OK` を確認した
- Chromiumで加速度と位置のピンチ、ドラッグ、チュートリアル遷移を確認した
- Caddy上で加速度(3)の正解文が `正解！動いたのは y でした` のみになることを確認した
- `./scripts/deploy-physics-lab` を実行し、`/srv/www/physics-lab/` の配信HTMLと生成HTMLの一致を確認した

## Result

- 加速度、ジャイロ、磁気、位置で、共通の2軸ピンチと2次元ドラッグを使えるようになった
- 加速度と位置では、実際に操作できたときだけ必須チュートリアルが次へ進むようになった
- 加速度(3)の正解文からSVGタグ漏れの原因を除去し、配信先へ反映した
- 正本は `src/lab-common.js` と各 `src/*.html` に保たれ、ルートHTMLは生成物のままである

## Discussion

- 一般利用者向けには横軸だけを変えるより、指の動きに合わせて縦横を操作できる方が予測しやすい
- チュートリアル判定専用の別状態を持たず、共通グラフの操作イベントを使うことで表示と判定のずれを防いだ
- SVG漏れはSafari向けの二重描画処理を増やさず、不要な装飾を文章から外す最小修正とした
- 実機Safariを自動化できないため、コード上から原因文字列を除去し、Caddy上の同じ状態をChromiumで再現して確認した

## Closing

### 最終レビュー

- 原本 checklist: `docs/development-checklist.md`
- /tmp コピー: `/tmp/development-final-checklist-20260720-1030-graph-gestures.md`
- レビュー結果: SSoT、DRY、共通操作、状態と判定の一致、生成物更新、回帰テスト、Caddy反映を確認した。docs更新は不要
- 未通過時の扱い: close せず修正または Discussion へ戻す

- 状態: done
- 次のゲート: なし
- close 条件:
  - [x] Spec から外れていない
  - [x] 実装成果が Result に記録されている
  - [x] 最終レビューを実施した
  - [x] 未記録の作業ログが記録済み
