---
status: done
format: rough-dev
devStage: closing
priority: normal
scheduled: 2026-07-20T11:30:00+00:00
dateCreated: 2026-07-20T11:30:00+00:00
dateModified: 2026-07-20T11:30:00+00:00
tags:
  - task
  - archived
---

# 2026年7月20日 点の値を一時停止中だけ表示する

> 状態：devStage closing
> 次のゲート：なし

## 概要

- When: 2026年7月20日
- Where: `/home/exedev/physics-lab`
- Who: AIエージェント
- What: 共通時系列グラフの点の値を、一時停止中だけ表示する
- Why: 計測中は現象の観察へ集中し、停止後に落ち着いて値を読めるようにするため

## Spec

### 決定事項

- 値読み可否は `src/lab-common.js` の `canReadValue` で共通判定する
- 計測中の線タップでは、点、ツールチップ、選択時刻の値を表示しない
- 一時停止後は、拡大グラフの線をタップして従来どおり値を読める
- 拡大、ピンチ、ドラッグ、通常表示への復帰は計測中も維持する
- 加速度、ジャイロ、磁気、位置へ同じ規則と案内を適用する

### 却下案

- 線タップ時に自動的に計測を停止する案
- ページごとに独自の値読み可否判定を持つ案
- 計測中も値を表示し、ヒントだけで一時停止を勧める案

### 未決事項

- なし

### 完了条件

- [x] 計測中の線タップで点の値が表示されない
- [x] 一時停止後の線タップで点の値が表示される
- [x] 値読み以外のグラフ操作は計測中も使える
- [x] 4つのグラフページで同じ規則と案内を使う
- [x] 生成、全テスト、ブラウザ確認、Caddy配信物の一致を確認する

### 設計レビュー

- 原本 checklist: `docs/development-checklist.md`
- /tmp コピー: `/tmp/development-design-checklist-20260720-1130-paused-values.md`
- レビュー結果: SSoT、共通部品優先、状態と表示の一致、ページ間の操作統一を満たす設計であることを確認した
- 未通過時の扱い: 実装へ進まず Spec または Discussion へ戻す

## 作業ログ

- `aba358c` で、点の値を一時停止中だけ表示する設計を記録した
- `src/lab-common.js` に任意の `canReadValue` コールバックを追加した
- `createXYZLab` は `!running`、位置ページは `!isRunning()` を値読み条件として渡すようにした
- 計測再開時に既存マーカーを消し、計測中のカーソル表示と `onMarker` 通知を止めた
- 加速度、位置、ジャイロ、磁気のヒントを「止めてから値を読む」へ統一した
- 位置チュートリアルへ、値読み直前の一時停止ステップを追加した
- `e0c1511` で実装、生成HTML、テスト、UX方針をコミットした
- `python3 -m unittest discover -s tests -v` で `35 tests OK` を確認した
- `git diff --check` が成功した
- 生成HTMLと `/srv/www/physics-lab/` の全HTMLが一致することを確認した
- Caddy上の加速度グラフで、計測中はツールチップなし・拡大状態維持、一時停止後は `0.0 m/s² @ 0.05s` が表示されることを確認した

## Result

- 共通時系列グラフは、計測中に線をタップしても点の値を表示しなくなった
- 一時停止後は同じ操作で点、ツールチップ、時刻付きの値を表示できる
- 加速度、ジャイロ、磁気、位置の案内と値読み条件が共通化された
- 値読み制約は `src/lab-common.js` を正本とし、生成HTMLへ埋め込まれている

## Discussion

- 自動停止は利用者の意図しない状態変更になるため採用しなかった
- 線の近くをタップした判定自体は計測中も残し、値を出せない線タップで通常表示へ戻らないようにした
- グラフ右上の現在値は計測の観察に必要なため、今回の非表示対象に含めなかった

## Closing

### 最終レビュー

- 原本 checklist: `docs/development-checklist.md`
- /tmp コピー: `/tmp/development-final-checklist-20260720-1130-paused-values.md`
- レビュー結果: SSoT、DRY、共通操作、状態と表示の一致、生成物更新、35テスト、Caddy上の実操作を確認した。追加のdocs更新は不要
- 未通過時の扱い: closeせず修正またはDiscussionへ戻す

- 状態: done
- 次のゲート: なし
- close 条件:
  - [x] Specから外れていない
  - [x] 実装成果がResultに記録されている
  - [x] 最終レビューを実施した
  - [x] 未記録の作業ログが記録済み
