---
status: done
format: rough-dev
devStage: closing
priority: normal
scheduled: 2026-07-20T00:14:55.000+00:00
dateCreated: 2026-07-20T00:14:55.000+00:00
dateModified: 2026-07-20T06:42:10.000+00:00
tags:
  - task
  - archived
---

# 2026年7月20日 位置ページのグラフ見た目を加速度ページにそろえる

> 状態：devStage closing
> 次のゲート：なし

## 概要

- When: 2026年7月20日
- Where: `/home/exedev/physics-lab`
- Who: AIエージェント
- What: `location` の時系列グラフカードだけを `acceleration` とほぼ同じ見た目へ寄せる
- Why: ページ間で学習導線をそろえつつ、地図カードは `location` 固有の役割として残すため

## Spec

### 決定事項

- `location` の上段グラフだけに `graph-card` スタイルを導入する
- `location` の下段地図は `map-card` として別見た目を維持する
- 軸色も `acceleration` と同じ `x=青 y=緑 z=黄` に戻す
- ズーム入口は `head` ではなく `グラフ本体タップ` へ寄せる

### 却下案

- 地図カードまで `acceleration` と同じカード見た目にそろえる案
- 見た目だけでなく地図の責務まで共通化する案

### 未決事項

- なし

### 完了条件

- [x] `graph-card` と `map-card` が分離されている
- [x] `location` の時系列グラフカードが `acceleration` と同系統の見た目になっている
- [x] テストと build が通る

### 設計レビュー

- 原本 checklist: `docs/development-checklist.md`
- /tmp コピー: `/tmp/development-design-checklist-20260720-0642-location-style.md`
- レビュー結果: `既存デザインシステムへ寄せる`、`共通部品優先`、`位置ページ固有の地図役割は残す` を満たす設計であることを確認した
- 未通過時の扱い: 実装へ進まず Spec または Discussion へ戻す

## 作業ログ

- `tests/test_location_page.py` に `test_location_time_series_cards_match_acceleration_style` を追加して RED を作った
- `python3 -m unittest discover -s tests -p 'test_location_page.py' -v` で fail を確認した
- `src/location.html` に `graph-card` / `map-card` を追加し、時系列グラフだけ `acceleration` 風カードへ寄せた
- `createTimeSeriesCharts()` の `enableHeadToggle` は使わず、ズーム入口をグラフ本体側へそろえた
- `python3 build.py` を実行して生成物を更新した
- `python3 -m unittest discover -s tests -v` を実行し、`17 tests OK` を確認した
- `git diff --check` を実行し、問題なしを確認した
- 実装コミット: `402be0d` `style: align location graphs with acceleration cards`

## Result

- `location` の `変位 / 速度 / 加速度` グラフカードは `acceleration` にかなり近い見た目になった
- `location` の `地図カード` は別見た目のまま残した
- 学習導線はそろえつつ、`location` 固有の下段地図役割は維持できた

## Discussion

- 今回は見た目寄せに留め、地図や数値カードの再設計までは広げなかった
- `location` を全部 `acceleration` にそろえるのではなく、`時系列グラフだけ` をそろえるのが境界として妥当だった

## Closing

### 最終レビュー

- 原本 checklist: `docs/development-checklist.md`
- /tmp コピー: `/tmp/development-final-checklist-20260720-0642-location-style.md`
- レビュー結果: `UX-policy` と `coding-rules` の観点で、共通性とページ固有性の切り分けを確認した。生成物更新と回帰確認も済み
- 未通過時の扱い: close せず修正または Discussion へ戻す

- 状態: done
- 次のゲート: なし
- close 条件:
  - [x] Spec から外れていない
  - [x] 実装成果が Result に記録されている
  - [x] 最終レビューを実施した
  - [x] 未記録の作業ログが記録済み
