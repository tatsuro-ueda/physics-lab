# Checklist Application 2026-07-19

Scope: 公開中の `index / acceleration / stopwatch / location` と、`docs/architecture.md`、`docs/repository-structure.md`、`docs/UX-policy.md`、`docs/coding-rules.md` を対象に適用した。非表示ページは対象外とする。

Legend: `OK` = 現状の実装または文書で満たす。`要改善` = 直すべき不足がある。`要確認` = 実機・授業観察・ユーザー確認なしでは断定できない。

## Development Checklist

- OK: 同じ仕様の正本を `src/` と `docs/` に寄せ、ルートHTMLは生成物として扱っている。
- 要改善: ページ固有コードは入力取得と変換だけに絞る原則に対し、`src/location.html` は汎用的な描画責務まで抱えている。
- OK: チュートリアル、オンボーディング、縦向きガードは共通部品へ寄せている。
- 要改善: 「加速度のグラフ部品をなるべく位置でも使う」原則に対し、`src/location.html` は `lab-common.js` を使わず独自の `drawGraph` と `drawTrack` を持つ。
- OK: 画面上の成功とチュートリアル判定は `tut.tick` / `tut.event` で接続している。
- OK: 権限要求は開始操作を起点にしている。
- OK: `visibilitychange` で計測停止と資源解放を行っている。
- OK: 復帰時に計測を自動再開しない。
- OK: 実行時に外部CDNや外部ネットワークへ依存していない。
- 要確認: 「30秒で何を見るか・何を押すか・何が起きるかが分かる」は設計上は満たすが、このターンでは実観察していない。
- OK: 物理現象と計測結果が主役で、設定が前に出すぎていない。
- OK: 状態文、ボタン、エラー文は現在状態と次の一手を示している。
- OK: 公開中3ページのオンボーディングは最大3ステップで、最後に `#missionBtn` を指している。
- OK: チュートリアルの表示上の成功と合格判定は同じ状態またはイベントを使っている。
- OK: 編集対象は `src/` の正本で、ルートHTMLを直接編集していない。
- OK: `python3 build.py` はこのターンで成功し、生成物を再確認した。
- OK: `git diff --check` はこのターンで成功した。
- 要確認: `python3 -m unittest discover -s tests -v` はこのターンで成功したが、HTTPS 実機確認は再実施していない。

## Customer Checklist

- OK: 既存授業のどの不満を解くかは `docs/customer-journey.md` で一文にまとまっている。
- OK: customer が教員、user が生徒である整理は文書上で一貫している。
- OK: 価値の重心は「高機能」より「授業が止まらない」「生徒が自走する」に置かれている。
- 要確認: 生徒が30秒以内に始められるかは設計意図としては強いが、このターンで授業観察はしていない。
- OK: オンボーディングとチュートリアルにより、教員の長い口頭説明を減らす設計になっている。
- OK: 権限差、端末差、失敗時の回復導線を授業リスクとして扱っている。
- OK: 公開中3ページは黄色ヘッダー、開始導線、チュートリアル導線を再利用している。
- OK: トップページと各 journey 文書により、他教員へ短時間で説明できる形になっている。
- OK: 専用器具や大きな予算がなくても価値が成立する。
- OK: `PhET`、`phyphox`、演示授業との比較と負ける条件は文書化されている。

## User Checklist

- 要確認: 30秒以内に「何を見るか・何を押すか・何が起きるか」を理解できるかは、実生徒観察なしでは断定しない。
- OK: 公開中3ページは主要操作が見える位置にあり、説明を読まないと動けない構造を避けている。
- 要確認: 「最初の1回で測れたと実感できる」は location / stopwatch でテスト根拠があるが、実機の生徒体験としては再確認していない。
- OK: 自分の動きや音とグラフ・計時結果が結びつく設計になっている。
- OK: 位置ページでは、変位・速度・加速度を連続した体験として見比べられる。
- OK: 設定より観察を主役にする構成は保てている。
- 要確認: 操作役だけが理解する構図を避けられているかは、班活動の実観察が必要である。
- 要確認: 権限拒否、数値の跳ね、端末差が不安を増やしにくいかは、設計上は配慮済みだが生徒確認が要る。
- 要確認: 別ページでも同じ感覚で使え、再挑戦したくなるかは、実授業での継続利用確認が必要である。
- 要確認: 演示、`PhET`、`phyphox`、紙と手計測より「自分で確かめた感じ」が強いかは、生徒インタビューなしでは断定しない。

## Top Findings

- 主要な構造ギャップは `src/location.html` に残る独自グラフ描画である。`lab-common.js` への共通化余地がまだ大きい。
- `customer / user` 側の大半は「設計としては通る」が、「授業で本当にそう感じるか」は別の確認軸である。
- このターンの機械的検証は `python3 -m unittest discover -s tests -v` と `python3 build.py` の成功までで、実機 HTTPS 体験確認は未実施である。
