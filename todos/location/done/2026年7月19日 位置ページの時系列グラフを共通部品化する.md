---
status: done
format: rough-dev
devStage: closing
priority: normal
scheduled: 2026-07-19T23:50:05.000+00:00
dateCreated: 2026-07-19T23:50:05.000+00:00
dateModified: 2026-07-20T06:42:10.000+00:00
tags:
  - task
  - archived
---

# 2026年7月19日 位置ページの時系列グラフを共通部品化する

> 状態：devStage closing
> 次のゲート：なし

## 概要

- When: 2026年7月19日
- Where: `/home/exedev/physics-lab`
- Who: AIエージェント
- What: `location` の時系列グラフを `lab-common.js` の共通部品へ載せ替える
- Why: checklist の `SSOT / DRY / 共通部品優先` を満たし、加速度ページのグラフ基盤を位置ページでも再利用するため

## Spec

### 決定事項

- `src/lab-common.js` に `createTimeSeriesCharts()` を追加する
- 共通部品に `uPlot描画 / タップ値読み / 拡大縮小 / ピンチ・パン` を集約する
- `src/location.html` には `GPS取得 / 位置→速度→加速度の導出 / 地図描画` だけを残す
- 既存の `createXYZLab()` も同じ共通部品の上へ載せる

### 却下案

- `location` だけ独自 canvas 描画を残す案
- 地図描画まで今回一気に共通化する案

### 未決事項

- なし

### 完了条件

- [x] `createTimeSeriesCharts()` が `src/lab-common.js` にある
- [x] `src/location.html` が共通グラフ API を使う
- [x] 全テストと build が通る

### 設計レビュー

- 原本 checklist: `docs/development-checklist.md`
- /tmp コピー: `/tmp/development-design-checklist-20260720-0642-location-commonization.md`
- レビュー結果: `ページ固有コードは入力取得と変換だけに寄せる`、`生成物直編集禁止`、`位置でも加速度のグラフ部品を再利用する` を満たす方針で進めた
- 未通過時の扱い: 実装へ進まず Spec または Discussion へ戻す

## 作業ログ

- skill・tool・MCP確認済み: `tasknotes / tn-backfill / tn-close / test-driven-development` を確認した
- `tests/test_lab_common.py` と `tests/test_location_page.py` に先に failing test を追加した
- `python3 -m unittest discover -s tests -p 'test_lab_common.py' -v` と `python3 -m unittest discover -s tests -p 'test_location_page.py' -v` で RED を確認した
- `src/lab-common.js` に `createTimeSeriesCharts()`、`injectLabUplotStyle()`、`toggleCardZoom()` を追加した
- `src/location.html` の独自 `drawGraph` / `seriesMinMax` / クリック処理を撤去し、`graphs.pushPoint()` 配線へ差し替えた
- `python3 build.py` を実行して生成物を更新した
- `python3 -m unittest discover -s tests -v` を実行し、`16 tests OK` を確認した
- `git diff --check` を実行し、問題なしを確認した
- 実装コミット: `55f4537` `refactor: share time-series charts with location page`

## Result

- `src/lab-common.js` に位置ページも使える時系列グラフ共通部品を追加した
- `src/location.html` の上段グラフは共通部品を使う構成になった
- `acceleration / gyroscope / magnetometer` も同じグラフ基盤の上にそろった
- `location` 固有の責務は `GPS取得 / 物理量導出 / 下段地図` に整理できた

## Discussion

- 下段地図の共通化は今回の範囲外にした
- 物理の意味が違う部分まで共通部品へ押し込みすぎない、という `coding-rules.md` の境界は守った

## Closing

### 最終レビュー

- 原本 checklist: `docs/development-checklist.md`
- /tmp コピー: `/tmp/development-final-checklist-20260720-0642-location-commonization.md`
- レビュー結果: `SSOT / DRY / 共通部品優先 / 状態の一貫性 / 生成物直編集禁止 / 検証` を確認した。位置ページ固有の残し方も妥当
- 未通過時の扱い: close せず修正または Discussion へ戻す

- 状態: done
- 次のゲート: なし
- close 条件:
  - [x] Spec から外れていない
  - [x] 実装成果が Result に記録されている
  - [x] 最終レビューを実施した
  - [x] 未記録の作業ログが記録済み
